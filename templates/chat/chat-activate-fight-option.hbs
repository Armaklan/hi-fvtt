<div>
  <img class="chat-icon" src="{{img}}" alt="{{name}}" />
  <h2 class="good"><strong>{{name}}</strong></h2>
</div>

<div class="flexrow">
  {{#if state}}
    {{localize "HI.chat.fightactive" name=name foName=foName}}
  {{else}}
    {{localize "HI.chat.fightunactive" name=name foName=foName}}
  {{/if}}
</div>